{
  "name": "trips-humlovi",
  "version": "2.0.0",
  "description": "Cesty Humlových — rozcestník + itineráře",
  "scripts": {
    "start": "npx --yes serve -s . -l tcp://0.0.0.0:${PORT:-8080}"
  }
}
